
exports.init = function(io) {
  io.sockets.on('connection', function (socket) {
    try {
      /**
       * create or joins a room
       */


      /**
       * send chat messages
       */

      /**
       * disconnect
       */
      
    } catch (e) {
    }
  });
}
